# progress

> 세션 시작 때 여기부터 읽습니다. 앞쪽이 **지금 · 다음 · 어긋난 것**, 뒤쪽이
> **왜 이렇게 됐는지**입니다.
>
> 파일 목록과 함수 이름은 적지 않습니다 — 코드와 주석에 있고, 적어두면 먼저
> 낡습니다. 여기 남기는 건 **코드를 읽어도 알 수 없는 것**뿐입니다. 판단의 근거,
> 문서와 어긋난 지점, 다시 재보지 않아도 되는 실측값.

## 지금 상태 (2026-08-04)

| 영역       | 상태                                                                           |
| ---------- | ------------------------------------------------------------------------------ |
| 스캐폴딩   | Next 16.2.12 / React 19.2 / TS / Tailwind v4, `src/` 구조                      |
| 인증       | 구글 OAuth 실제 왕복 통과. 가입 트리거 동작 확인                               |
| DB         | 기획서 §4 테이블 6개 + RLS 전부 적용·검증 완료                                 |
| 알라딘 §7  | 검색·담기·표지 복사·대표색 **실제 왕복 통과**. 검증 항목 완료                  |
| 판형       | 알라딘의 가로·세로 뒤바뀜 보정까지 구현 (§5 규칙)                              |
| 활자       | IBM Plex Sans KR + **MaruBuri(문장)** 자체 서빙(next/font, swap)               |
| 화면       | `/` 랜딩(디자인 이전) · `/shelf` · `/shelf/[id]` · `/underlines` 구현          |
| 오늘의밑줄 | 책장 맨 위 한 장(§5·§6). 하루 고정 + 오래 안 본 것 우선 + `last_shown_at` 갱신 |
| 네비       | 상단 네비 52px + 손으로 그은 활성 표시. 항목은 책장 · 밑줄 + 검색 아이콘       |
| 서명       | 손그림 선(`ScribbleLine`) — 카드 밑줄은 정적 전폭 물결. 그어지는 연출은 순간용 |

Supabase 전용 프로젝트 `moseori` (`ccfgjaxaylwwdfsspdys`, Seoul).
마이그레이션 4개 적용됨 — `20260730000000_profile`, `20260730010000_core_schema`,
`20260730020000_cover_storage`, `20260730030000_book_cover_size`.

원격 데이터는 `book` 5행(표지·대표색 전부 채워짐) + 본인 서재 1권.
검증용 계정은 만들었다가 지웠습니다.

**아직 눈으로 못 본 것: 책장의 실제 렌더.** 격자·탭·네비 모두 로그인 세션이
있어야 보입니다. 서재가 1권이고 그 책 상태가 `reading`이 아니면 기본 탭이 비어
보입니다 — 탭을 옮겨 보세요.

---

## 남은 것

**다음 세션 후보**

1. **랜딩** `/` — 네비가 없는 유일한 화면입니다. 아직 디자인 이전 상태

**두고 온 것**

- **리뷰의 렌더된 표시가 없습니다** (세션 5). `**굵게`\*\*는 저장·왕복되지만 상세에서는
  textarea 원문으로만 보입니다. 세션 8에서 굵게 버튼까지 걷어냈으므로 지금 리뷰는
  **완전히 평문 입력**입니다 — 서식을 쓰려면 `**`를 손으로 칩니다.
  굵게가 실제로 굵게 보이는 읽기 뷰가 생기기 전까지는 `**`가 화면에 그대로 보입니다
- **기존 밑줄에 코멘트를 나중에 더 다는 UI가 없습니다** (세션 5). 밑줄을 그을 때
  코멘트 하나는 같이 담기고 목록에 시간순으로 보이지만, 덧붙이는 입력은 없습니다
- **밑줄의 코멘트는 여전히 고치거나 지울 수 없습니다** (세션 8). 밑줄·리뷰의
  수정·삭제는 붙였지만 `passage_comment`는 사용자 지시로 이번 범위 밖입니다.
  밑줄을 지우면 코멘트는 살아 있는 채로 조회에서 사라집니다(밑줄을 통해서만
  닿기 때문). 코멘트 편집을 만들 때 이 잔여 행을 어떻게 볼지 정해야 합니다
- **RLS 런타임 검증이 아직 남았습니다** (세션 2에서 미룬 것). 정책 정의는 맞지만
  로그인 세션 두 개로 남의 `shelf_item`이 안 보이는지는 아직 안 봤습니다.
  이제 데이터가 있으니 확인할 수 있습니다 (계정 두 개 만드는 방법은 아래)
- **밑줄 개수를** `passage(id)`**를 받아 세고 있습니다.** PostgREST의 `passage(count)`
  집계로 바꾸면 전송량이 줄지만, 지금은 서재가 작고 확실한 쪽을 택했습니다.
  밑줄이 수백 개 쌓이면 바꾸세요
- **인증 화면이 늘어나면 route group으로 묶으세요.** 지금은 상단 네비를
  `shelf/layout.tsx`와 `underlines/page.tsx`에서 각각 씁니다. 아카이브·설정이
  생기면 `(app)/layout.tsx` 하나로 모으는 게 맞습니다
- **표지 아래 제목을 줄 수로 자르지 않았습니다.** 긴 제목은 4~5줄이 됩니다.
  격자는 안 흔들리지만(행 높이는 가장 큰 셀이 정합니다) 줄 수 제한이 필요한지는
  책이 20권쯤 쌓인 뒤에 보세요. 기획서에 없는 규칙입니다
- **표지를 다시 받아오는 기능을 만들면 경로에 버전을 붙여야 합니다.**
  공개 버킷은 CDN이 캐시하므로 같은 경로에 덮어써도 잠시 옛 파일이 나옵니다.
  표지는 한 번 쓰고 안 바꾸니 지금은 문제가 아닙니다
- `/settings` — 개인정보처리방침 링크(§5)는 배포 전에 필요하고,
  **로그아웃 버튼이 갈 곳이 없어 지금** `/shelf` **맨 아래에 있습니다**
- `npm audit` high 12건. 전부 `eslint` 하위 `brace-expansion` 계열 **개발 의존성**이라  
  런타임에 안 실립니다. `--force`는 eslint 10 메이저 업그레이드
- 강조색 `#C4573A`는 여전히 미사용 (§10에서 역할 미정)

---

## 문서와 어긋난 것

여기를 빠뜨리면 문서가 서서히 거짓말이 됩니다. **지금도 유효한 것만** 둡니다 —
해소된 항목은 지웁니다.

| 어디                      | 어긋남                                                                                  | 왜                                                                       |
| ------------------------- | --------------------------------------------------------------------------------------- | ------------------------------------------------------------------------ |
| 기획서 §4 `review` UNIQUE | 컬럼 제약이 아니라 부분 인덱스 (`where deleted_at is null`)                             | soft delete와 충돌합니다. 지운 리뷰가 자리를 차지해 같은 책에 다시 못 씀 |
| design.md 176px 슬롯      | 상한이 아니라 **최소 높이**로 읽었습니다                                                | `높이 = size_height × 0.7`과 부딪힙니다 (아래 세션 4)                    |
| design.md 표지 격자       | 720px 미만 3열 / 532px 미만 2열                                                         | 4열 664px이 컨테이너 안쪽 폭을 정확히 채웁니다. 셀·간격은 안 바꿨습니다  |
| design.md에 없는 값       | 네비 항목 간격 22px · 탭↔개수 5px · 페이지 여백 56px · 탭↔격자 36px · 네비 선 높이 10px | 규격이 없는 자리입니다. 22 / 36px은 기존 값에 맞췄습니다                 |
| design.md에 없는 값 (7)   | 오늘의 밑줄 카드↔라벨 16px · 라벨↔탭 48px(`mb-12`) · 상태 탭 모바일 가로 스크롤         | 오늘의 밑줄 위치·모바일 넘침 자리. 기존 리듬(라벨·여백)에 맞췄습니다     |
| 기획서 §3 정규화 범위     | 명도 `88.5~90.5%` / 채도 `16~29%`로 1%씩 좁힘                                           | 16진수 반올림 때문에 저장된 색이 `88~91` / `15~30`을 벗어납니다 (세션 3) |
| 기획서 §9 스택            | `sharp` 한 개 추가                                                                      | 대표색 추출과 표지 픽셀 검사                                             |
| 기획서 §5 로그아웃        | 설정이 아니라 `/shelf` 맨 아래                                                          | `/settings`가 아직 없습니다                                              |
| 기획서 §6 오늘의 밑줄     | 밑줄 3개 미만이면 "밑줄을 그어보세요" 안내 대신 **아무것도 안 보임**                    | 세션 7, 사용자 지시. §6은 안내 문구를 요구하지만 두지 않음               |
| 기획서 §6 리뷰 에디터     | "textarea에 굵게 버튼 하나" 중 **굵게 버튼을 없앴습니다**                               | 세션 8, 사용자 지시. 서식 자체(`**굵게**` 저장·왕복)는 그대로입니다      |
| 기획서에 없는 UI          | 밑줄·리뷰의 **고치기 / 지우기** (인라인 2단계 확인)                                     | 세션 8, 사용자 요청. §5·§6에 편집·삭제 화면 규정이 없어 물어보고 진행    |
| design.md 하지 말 것      | 밑줄 카드 오른쪽 위의 **`×`** — 아이콘 금지에 걸립니다                                  | 세션 8, 사용자 지시. SVG가 아니라 글자로 그려 활자 규격 안에 뒀습니다    |
| 기획서 §5 밑줄 섞기       | 섞기 버튼을 넣지 않았습니다                                                             | 세션 6, 사용자 지시로 제외. 남은 것 3번에 복구 방법 적어둠               |
| 기획서 §5 `/` 랜딩        | 활자·간격이 Tailwind 기본 스케일 위의 임시값                                            | 디자인 이전 상태입니다 (`/shelf/[id]`는 세션 5에서 규격 적용)            |
| `style_desc`              | 알라딘이 주는 `"미확인"`이 그대로 들어갑니다                                            | 화면에 그대로 쓸지는 판형 표시를 만들 때 판단                            |

`review` UNIQUE는 **§4 문구를 고칠지 판단을 보류한 상태입니다.** 고친다면
"살아있는 리뷰 기준 UNIQUE"로 적는 게 맞습니다.

**승인받아 문서를 고친 이력**

세션 3 (표지 500px)

- 기획서 §7 "표지 — 200px가 천장입니다" → "500px를 받을 수 있습니다". 검증 항목도
  결과로 교체
- CLAUDE.md 절대 규칙
- design.md 원칙 2 — **원칙("표지는 주인공이 아닙니다")은 남기고 근거만** 고쳤습니다.
  주인공이 문장이라는 건 픽셀 사정과 무관한 결정이라서입니다
- §4 표에 `cover_width` / `cover_height` 추가, §5에 판형 판정 규칙 추가 (물어보고 진행)

세션 5 (별점·완독 분리)

- 기획서 §6 "별점과 완독" → "별점". 상태–별점 연동(완독 시 별점 묻기, 첫 별점이
  완독 유발, 작은 반응)을 전부 걷어내고 **둘을 독립**으로 다시 적었습니다.
  코드가 먼저 바뀌었고(사용자 지시) 문서를 뒤따라 맞췄습니다

세션 6 (밑줄 카드 · 손그림 선 — 사용자 명시 승인으로 design.md 수정)

- §손으로 그은 선 — 한 마디 → 폭÷~160px 다-마디 물결(Catmull-Rom 연결), 진폭
  마디점 ±2.5 / 끝점 ±2, 높이 12px, "카드 폭 끝까지 이어지는 구분선" 명시.
- §모션 — "카드 등장에는 그어지는 연출을 쓰지 않는다(처음부터 정적 전폭). 그 연출
  (0.7초 dashoffset)은 저장 완료 같은 순간용으로 남긴다."

---

## 알아두면 시간 아끼는 것들

`middleware`**가 아니라** `proxy`**.** Next 16이 `middleware` 파일 규약을 deprecate하고
이름을 바꿨습니다. API는 같습니다. Supabase 공식 문서는 아직 `middleware`로 적혀 있으니
문서를 볼 때 헷갈리지 마세요.

**세션 판정은** `getClaims()`**.** `getSession()`은 서버에서 신뢰하지 않습니다.

**anon 키는 publishable key(**`sb_publishable_...`**)를 씁니다.**
환경변수 이름은 기획서 §9대로 `NEXT_PUBLIC_SUPABASE_ANON_KEY` 유지.

**익명 키로 테이블을 조회하면** `200 []`**가 정상입니다.** 정책이 전부
`to authenticated`라 RLS가 아무것도 주지 않습니다. 행을 눈으로 보려면
`service_role` 키가 필요한데, `supabase projects api-keys`는 새 `sb_secret_...`을
마스킹해서 줍니다 — 레거시 `service_role` JWT를 쓰거나 대시보드에서 복사하세요.

**쿼리 문법만 확인할 때는 익명 키가 오히려 편합니다.** RLS로 `[]`가 오지만 문법이
틀리면 400이 옵니다. **임베드한 필터가 정말 자식 테이블에 걸리는지**도 없는 컬럼을
넣어보면 압니다 — `passage.nope=is.null` → `column passage_1.nope does not exist`.

**Supabase가 실제로 들고 있는 OAuth 값 확인법.**

```
GET {SUPABASE_URL}/auth/v1/authorize?provider=google&redirect_to=...
→ 302 Location에 client_id, redirect_uri, scope가 그대로 들어 있음
```

세션 1-c에서 구글 client ID가 앞부분(`1974351245-`)이 잘린 채 저장돼 있어
`invalid_client`가 났습니다. 구글 client ID는 항상
`프로젝트번호-무작위문자.apps.googleusercontent.com` 형태입니다.

`toy` **프로젝트를 가리키고 있지 않은지 확인.**
`cat supabase/.temp/project-ref` → `ccfgjaxaylwwdfsspdys`

**로그인이 필요한 경로를 스크립트로 검증하는 법.** 구글 OAuth는 스크립트로 못
돌리지만, 검증용 계정을 만들면 됩니다. 세션 3에서 API 전체를 이렇게 확인했습니다.

```
1. service_role로 계정 생성   POST /auth/v1/admin/users  {email, password, email_confirm:true}
2. 비밀번호로 로그인          POST /auth/v1/token?grant_type=password   → access_token
3. 쿠키로 만들어 curl에 붙임  sb-<project-ref>-auth-token=base64-<base64(세션 JSON)>
4. 끝나면 계정 삭제           DELETE /auth/v1/admin/users/<id>
```

`shelf_item`·`profile`은 `auth.users` cascade로 같이 사라지고 `book`은 남습니다
(공용 마스터라서 정상입니다). `UID`**를 셸 변수 이름으로 쓰지 마세요** — zsh 예약
변수라 대입이 실패합니다.

**공개 버킷은 CDN이 캐시합니다.** 같은 경로에 파일을 덮어써도 공개 URL이 한동안
옛 파일을 줍니다. 세션 3에서 이걸 모르고 버그로 착각했습니다. 실제 내용을 보려면
인증 경로(`/storage/v1/object/cover/...` + service_role)나 쿼리스트링으로 캐시를
피하세요.

`supabase db dump`**는 도커가 필요합니다.** 적용된 스키마를 원격에서 대조할 때는
Management API의 query 엔드포인트로 직접 조회하세요 (세션 2에서 그렇게 했습니다).

**Next 16** `dev`**는 데몬으로 뜹니다.** 실행한 명령이 먼저 끝나고 서버는 남습니다.
두 번 실행하면 `Another next dev server is already running`과 함께 PID를 알려줍니다.
로그는 `.next/dev/logs/next-development.log` (브라우저 콘솔 경고까지 들어옵니다).
`pkill -f "next dev"`**는 이미 떠 있던 서버까지 잡습니다** (세션 4에서 한 번
그랬습니다). 포트로 골라 죽이세요.

`next/font`**로 한글 폰트를 쓸 때** `subsets`**를 지정하면 안 됩니다.** next/font가 아는
서브셋은 `latin` / `latin-ext`뿐입니다. 지정하면 **빌드는 통과하고 한글만 시스템
폰트로 조용히 떨어집니다.** 비워두면 한글 서브셋까지 내려받습니다 — 대신
unicode-range가 246개로 쪼개지므로 `preload`는 끕니다.

---

## 알라딘과 표지 — 다시 재보지 않아도 되는 값

표본 18권으로 확인한 사실입니다.

- `OptResult=packing`**은 별도 협의 없이 됩니다.** 판형 결측 **0건** — §10의
  "판형 결측률" 우려는 최소한 유통 중인 책에서는 기우였습니다
  (절판·독립출판은 아직 표본 없음)
- 검색 응답에 `itemId`가 옵니다. `isbn13`·`cover`도 함께
- `/coverbig/`**는 404,** `/cover500/`**이 500×713px를 줍니다** (18/18 성공)
- **엔드포인트가** `https://`**로 응답합니다.** §7의 mixed content 이야기는 낡은
  정보였습니다. 키 때문에 서버 경유는 여전히 필요합니다
- **200을 받았다고 큰 이미지인 게 아닙니다.** 오래된 책에는 `/cover500/`에
  원본보다 작은 파일이 있습니다 — 『이해선 사진집』(`592934`)의 cover500은
  150×155, cover200은 200×207입니다. 바이트 수로는 안 걸러집니다(10KB로 멀쩡함).
  그래서 **실제 픽셀 너비가 300px 미만이면 폴백**합니다
- `accent_color`는 표본 18권 전수 검사에서 18/18 §3 범위 안
- **판형의 가로·세로가 뒤바뀐 채 오는 책이 18권 중 5권입니다**

| 책                     | 알라딘 판형 | 표지 이미지 |
| ---------------------- | ----------- | ----------- |
| 소설을 살다 (문고본)   | 178 × 110   | 200×324     |
| 대적기도 (문고본)      | 174 × 118   | 200×300     |
| 미디어의 이해 (문고본) | 188 × 128   | 200×292     |
| 브랜드 디자인          | 210 × 150   | 200×280     |
| 윤미네 집              | 193 × 153   | 200×279     |

**표지 상자 실측** (판형 보정 + 폭 148px 제한까지 적용한 최종값)

| 경우                     | 판형    | 표지 상자   |
| ------------------------ | ------- | ----------- |
| 문고본 (뒤바뀜 보정)     | 110×178 | 77 × 125px  |
| 신국판                   | 152×225 | 106 × 158px |
| 정사각 사진집            | 257×257 | 148 × 153px |
| 진짜 가로형 (이미지도)   | 257×188 | 148 × 108px |
| 판형 없음 → 152×225 기본 | —       | 110 × 158px |
| 표지 픽셀 없음           | 152×225 | 106 × 158px |

셀 높이가 108~158px로 벌어집니다 — §5가 원한 들쭉날쭉함입니다. **폭 제한이 높이
제한보다 먼저 걸리기 때문에 176px 슬롯을 넘는 책은 드뭅니다.**

---
